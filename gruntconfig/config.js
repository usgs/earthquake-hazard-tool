'use strict';

var config = {
  src: 'src',
  build: '.build',
  buildPort: 8500,
  dataPort: 8507,
  dist: 'dist',
  distPort: 8503,
  etc: 'etc',
  example: 'example',
  examplePort: 8504,
  liveReloadPort: 8509,
  templatePort: 8508,
  test: 'test',
  testPort: 8501
};

module.exports = config;
